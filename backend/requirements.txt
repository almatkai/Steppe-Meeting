fastapi>=0.115.0
uvicorn>=0.30.0
pydantic>=2.10.0
pydantic-settings>=2.7.0
python-multipart>=0.0.18
httpx>=0.28.0
python-docx>=1.1.0
docxtpl>=0.18.0
backoff>=2.2.1
lxml>=5.3.0
Pillow>=11.0.0
# Ships a static ffmpeg build as a wheel. This is what makes audio conversion
# and chunking work on a clean Windows machine with no system ffmpeg install.
imageio-ffmpeg>=0.5.1
# Runs Whisper inside this process, so transcription opens no socket at all.
# This is what makes the "no external API calls" guarantee true rather than
# just configured.
faster-whisper>=1.1.0
